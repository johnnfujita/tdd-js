/**
 * A function that adds numbers
 * @param {num} x - a number x
 * @param {num} y  - a number y
 * @returns {num} - the sum of both
 */
function add(x, y) {
    return x + y;
};

module.exports = add